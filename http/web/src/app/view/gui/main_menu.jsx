import React, {useState, useEffect, Fragment} from "react";

export default function Main_menu(props)
{
	useEffect(()=>{ return(()=>{}); }, []);

	return (
	<>
		<section>
			Main menu
		</section>
	</>
	);
}