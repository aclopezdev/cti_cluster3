import React, {useState, useEffect, Fragment} from "react";

export default function Footer(props)
{
	useEffect(()=>{ return(()=>{}); }, []);

	return (
	<>
		<section>
			Footer
		</section>
	</>
	);
}